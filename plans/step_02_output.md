# STEP_02 — Output Generator

Call `generate_final_output` to assemble the final JSON with:
- document_requests (with satisfied_specifications populated)
- stats showing how many specs were satisfied vs remaining

Then call `save_step_report`.
